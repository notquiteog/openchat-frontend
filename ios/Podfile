# Uncomment this line to define a global platform for your project
platform :ios, '15.0'

# CocoaPods analytics sends network stats synchronously affecting flutter build latency.
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

project 'Runner', {
  'Debug' => :debug,
  'Profile' => :release,
  'Release' => :release,
}

def flutter_root
  generated_xcode_build_settings_path = File.expand_path(File.join('..', 'Flutter', 'Generated.xcconfig'), __FILE__)
  unless File.exist?(generated_xcode_build_settings_path)
    raise "#{generated_xcode_build_settings_path} must exist. If you're running pod install manually, make sure flutter pub get is executed first"
  end

  File.foreach(generated_xcode_build_settings_path) do |line|
    matches = line.match(/FLUTTER_ROOT\=(.*)/)
    return matches[1].strip if matches
  end
  raise "FLUTTER_ROOT not found in #{generated_xcode_build_settings_path}. Try deleting Generated.xcconfig, then run flutter pub get"
end

require File.expand_path(File.join('packages', 'flutter_tools', 'bin', 'podhelper'), flutter_root)

flutter_ios_podfile_setup

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  share_handler_models_path = File.expand_path('../.symlinks/plugins/share_handler_ios/ios/Models', __FILE__)
  pod 'share_handler_ios_models', :path => share_handler_models_path if File.directory?(share_handler_models_path)

  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
  target 'RunnerTests' do
    inherit! :search_paths
  end
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
  end

  # OpenPGP FFI symbol retention (iOS only).
  #
  # The `openpgp` plugin resolves its Go bridge symbol at runtime via dlsym, so
  # nothing references it statically. A release build defaults to STRIP_STYLE =
  # all, which can drop it from the Runner executable's dynamic symbol table and
  # cause "Failed to lookup symbol 'OpenPGPBridgeCall'" on the first crypto call
  # (account creation). Stripping only non-global symbols keeps the external
  # symbols dlsym needs while still removing locals. This is safe regardless of
  # whether the plugin links via CocoaPods or Swift Package Manager.
  installer.aggregate_targets.each do |aggregate_target|
    aggregate_target.user_project.native_targets.each do |native_target|
      next unless native_target.name == 'Runner'
      native_target.build_configurations.each do |config|
        config.build_settings['STRIP_STYLE'] = 'non-global'
      end
    end
    aggregate_target.user_project.save
  end
end
